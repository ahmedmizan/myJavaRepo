package object.orientedconcept;

public interface School {

    public void building();

    public void teachingMethods();

    //  AC is a class and Interface is not a class
    // Sub-class extends AC but Sub-class implements interface
    // AC can have regular and abstract methods but interface can have only abstract methods
    // Sub-class can extend only one AC but sub-class can implement more than one interface

}
